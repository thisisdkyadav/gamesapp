import React from 'react';

const Loading = () => {
  return (
    <div className="loading-container">
      <div className="spinner"></div> // Add spinner or loading animation
      <p>Loading...</p>
    </div>
  );
};

export default Loading;